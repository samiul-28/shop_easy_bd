<h1><%= action_name == 'new' ? t('categories.new.title') : t('categories.edit.title') %></h1>
<%= render 'form', category: @category %>
<%= link_to t('products.show.back'), categories_path, class: "btn btn-secondary mt-2" %>
